Or :

First run this command : . /Users/eleni/.espressif/v5.5.3/esp-idf/export.sh

Or instead :

nano ~/.zshrc

At the end of the file :

alias idf='. /Users/eleni/.espressif/v5.5.3/esp-idf/export.sh'

Press Ctrl+O and then Ctrl+X

Open a new terminal

Ready to do : idf.py build monitor flash !!


the page is on eleniperpe.github.io and you have to connect throught bluefy cause normal bluetooth can not find my device.

then you connect and it works.